{
    "name": "team-1-repo",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
        "test": "jest --testTimeout=10000 --silent",
        "start": "nodemon backend/server.js",
        "temp": "cd .. && cd frontend && npm run build && cd .. && cd backend && npm i && npm start"
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "bcrypt": "^5.1.1",
        "cookie-parser": "^1.4.7",
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.21.0",
        "jsonwebtoken": "^9.0.2",
        "mongoose": "^8.7.0",
        "nodemon": "^3.1.7"
    },
    "jest": {
        "testEnvironment": "node",
        "coveragePathIgnorePatterns": [
            "/node_modules/"
        ]
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "jest": "^29.7.0",
        "supertest": "^7.0.0"
    }
}
